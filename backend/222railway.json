{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'if [ \"$SERVICE_ROLE\" = \"worker\" ]; then python -m celery -A task_queue.celery_app worker --loglevel=info --concurrency=2; else python -m uvicorn server:app --host 0.0.0.0 --port $PORT; fi'",
    "healthcheckPath": "",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
